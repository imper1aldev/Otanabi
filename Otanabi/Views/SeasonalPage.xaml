<Page
    x:Class="Otanabi.Views.SeasonalPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:usercontrols="using:Otanabi.UserControls"
    mc:Ignorable="d">

    <Grid x:Name="ContentArea">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition />
        </Grid.RowDefinitions>
        <Button Command="{x:Bind ViewModel.LoadSeasonalAnimesCommand}" Content="Load" />
        <usercontrols:AnimePaneControl
            x:Name="AnimeGrid"
            Grid.Row="3"
            Margin="0,15,0,10"
            HorizontalAlignment="Center"
            ItemsSource="{x:Bind ViewModel.AnimeList, Mode=OneWay}" />
    </Grid>
</Page>

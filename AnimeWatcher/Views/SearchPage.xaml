<Page
    x:Class="AnimeWatcher.Views.SearchPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:animations="using:CommunityToolkit.WinUI.UI.Animations"
    xmlns:controls="using:CommunityToolkit.WinUI.UI.Controls"
    xmlns:models="using:AnimeWatcher.Core.Models"
    mc:Ignorable="d">

    <Grid >
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"></RowDefinition>
            <RowDefinition Height="auto"></RowDefinition>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <AutoSuggestBox PlaceholderText="Search..."
                        
        QueryIcon="Find"
        Grid.Row="0"
        QuerySubmitted="{x:Bind ViewModel.OnAutoComplete}" Margin="0,0,0,40" />

        <controls:AdaptiveGridView
            Grid.Row="2"
            animations:Connected.ListItemElementName="itemThumbnail"
            animations:Connected.ListItemKey="animationKeyContentGrid"
            DesiredWidth="163"
            ItemHeight="240"
            IsItemClickEnabled="True"
            ItemClickCommand="{x:Bind ViewModel.ItemClickCommand}"
            ItemsSource="{x:Bind ViewModel.Source,Mode=OneWay}" 
            SelectionMode="None"
            StretchContentForSingleRow="False">
            <controls:AdaptiveGridView.ItemTemplate>
                <DataTemplate x:DataType="models:Anime" >
                    <Grid 
                        x:Name="itemThumbnail"
                        Padding="{StaticResource XSmallLeftTopRightBottomMargin}">
                        <Grid  HorizontalAlignment="Center" VerticalAlignment="Center">
                            <ToolTipService.ToolTip>
                                <ToolTip Content="{x:Bind title}" HorizontalOffset="20"/>
                            </ToolTipService.ToolTip>

                            <Image Source="{x:Bind cover}" Height="238" Width="163">
                            </Image>
                            <Border Background="#3b4348" VerticalAlignment="Bottom" >
                                <TextBlock Text="{x:Bind title}" Foreground="WhiteSmoke"></TextBlock>
                            </Border>

                        </Grid>
                    </Grid>
                </DataTemplate>
            </controls:AdaptiveGridView.ItemTemplate>
        </controls:AdaptiveGridView>
    </Grid>
</Page>
